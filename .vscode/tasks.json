{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "premake",
            "command": "premake gmake",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}